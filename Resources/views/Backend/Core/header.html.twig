<div id="header">
    <a href="{{ path('unifik_system_backend_dashboard') }}" class="unifik_egzakt">
        <img src="{{ asset('bundles/unifiksystem/backend/images/graphics/logo.png') }}" width="125" height="43" alt="{{ project_title }}" title="{{ project_title }}" />
        <span>bâti sur Symfony par Egzakt</span>
    </a>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {{ render(controller('UnifikSystemBundle:Backend/Navigation:App')) }}
    {% endif %}
</div>
