{% if sections %}
    <ul{% if attr|length %}{% for attribute, value in attr %} {{ attribute }}="{{ value }}"{% endfor %}{% endif %}>
        {% include 'EgzaktSystemBundle:Frontend/Navigation:by_code_level.html.twig' with { 'sections' : sections, 'level': 1, 'maxLevel' : maxLevel } %}
    </ul>
{% endif %}