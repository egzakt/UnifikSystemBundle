{% if routes|length %}
    <nav id="locales_navigation">
        <ul>
            {% for route in routes %}
                <li>
                    <a href="{{ route.url }}">{{ route.locale }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endif %}