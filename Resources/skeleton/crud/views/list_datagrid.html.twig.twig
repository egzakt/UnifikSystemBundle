{{ "{% extends '" ~ bundle_name ~ ":" ~ application ~ ":layout.html.twig' %}" -}}

{%- set hidden_fields = ['id', 'locale', 'active', 'featured', 'createdAt'] %}

{{ '{% block content_main %}' }}

{% if 'ordering' in fields|keys %}
    <script type="text/javascript">
       var url_ordre = '{{ "{{ path('" ~ route_prefix ~ "_order') }}" }}';
    </script>

{% endif %}

<div class="list-box panel panel-dark" style="margin-top: 10px;">
    <div class="panel-heading">
        <h3 class="panel-title">{{ "{% trans %}"}}{{ entity }}{{ "s List{% endtrans %}" }} <a class="btn btn-primary btn-xs ml20" href="{{ "{{ path('" ~ route_prefix ~ "_edit') }}" }}"><i class="fa fa-plus-square mr5"></i> {{ "{% trans %}Add a "}}{{ entity }}{{ "{% endtrans %}" }}</a></h3>
    </div>
    <div class="panel-body panel-body-dark">
        {{ '{{ grid(grid, "UnifikSystemBundle:Backend/Core:datagrid_theme.html.twig") }}' }}
    </div>
</div>

{{ '{% endblock content_main %}' }}
