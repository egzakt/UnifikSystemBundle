<div id="header">
    <a href="{{ path('unifik_system_backend_dashboard') }}">
        <img src="{{ asset('bundles/unifiksystem/backend/images/graphics/logo.jpg') }}" height="60" alt="{{ project_title }}" title="{{ project_title }}" />
    </a>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {{ render(controller('UnifikSystemBundle:Backend/Navigation:App')) }}
    {% endif %}
</div>
