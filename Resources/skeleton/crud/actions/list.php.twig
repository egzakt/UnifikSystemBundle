
    /**
     * Lists all {{ entity }} entities.
     *
     * @return Response
     */
    public function listAction()
    {
        ${{ entity_var }}s = $this->{{ entity_var }}Repository{% if 'order' in actions %}->findBy(array(), array('ordering' => 'ASC')){% else %}->findAll(){% endif %};

        return $this->render('{{ namespace|replace({'\\': ''}) ~ ':' ~ application ~ '/' ~ entity_class ~ '/' ~ entity|replace({'\\': '/'}) }}:list.html.twig', array(
            '{{ twig_entity_var }}s' => ${{ entity_var }}s
        ));
    }
