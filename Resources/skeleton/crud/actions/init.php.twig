    /**
     * Init
     */
    public function init()
    {
        parent::init();

        $this->{{ entity_var }}Repository = $this->getEm()->getRepository('{{ namespace|replace({'\\': ''}) ~ ':' ~ entity|replace({'\\': '/'}) }}');
    }
