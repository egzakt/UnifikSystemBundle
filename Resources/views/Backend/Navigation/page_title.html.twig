{% block title %}
{% spaceless %}
    {% for element in elements -%}
        {{ element.translate(app.request.locale)|striptags|truncate(100, true)|trans|raw }} -
    {%- endfor %}
    {{ project_title|trans }}
{% endspaceless %}
{%- endblock -%}