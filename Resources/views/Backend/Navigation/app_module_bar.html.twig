{% import 'FlexySystemBundle:System:mapping_macro.html.twig' as renderer %}

<ul id="app_global_bar">
    {% for mapping in mappings %}
        {{ renderer.generate(mapping) }}
    {% endfor %}
</ul>
