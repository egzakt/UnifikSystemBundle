<?php

namespace {{ namespace }}\Controller\{{ application }};

use Unifik\SystemBundle\Lib\{{ application }}\BaseController;

/**
 * Navigation controller.
 */
class NavigationController extends BaseController
{
    public function sectionModuleBarAction($_masterRoute)
    {
        $selected = (0 === strpos($_masterRoute, '{{ route_prefix }}'));

        return $this->render('{{ namespace|replace({'\\': ''}) ~ ':' ~ application ~ '/Navigation'}}:section_module_bar.html.twig', array(
            'selected' => $selected,
        ));
    }
}
